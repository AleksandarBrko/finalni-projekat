import React, { Component } from "react";

class Main extends Component {
  constructor(props) {
    // use super so we can access "this"
    super(props);
  }

  render() {
    return <div>Main</div>;
  }
}

export default Main;
